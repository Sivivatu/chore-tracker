{
  "buildCommand": "pnpm exec convex deploy --cmd-url-env-var-name VITE_CONVEX_URL --cmd \"pnpm build\"",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
