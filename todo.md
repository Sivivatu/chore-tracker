# Tasks to remember

- [x] need to add a lock on child page to prevent easy editing of routines & chores by child
- [x] add a chores list with chores that can be done on different time schedules. they refresh each week
- [x] update reward page to link to live datasets. also add reward editing so rewards can be customised.
- [x] find out how to gate signups - implement waitlist
- [ ] update theme to a fun child friendly theme on child pages and not codex style for adult pages
- [ ] add existing image file detection and minimisation processing
- [ ] enable UploadThing `awaitServerData`, return validated file metadata from `onUploadComplete`, and make the reward upload client consume the server-controlled response
- [x] need parent invite process to join additional parents to a family.
- [x] update the settings page to allow the setting of names
- [x] parent pin reset options in the settings page
- [x] connect dashboard to actual data in backend
- [ ] implement reward redemption process - child selects, parent approves
- [ ] do i want to add a behaviour page where bad behaviour can be tracked and it loses reward points?
- [ ] Behaviour bonuses, positive rewards for good behaviour, negative for bad. should be fixed amounts depending for 1-5 level of good/bad. free text for the reasons with tagging.
